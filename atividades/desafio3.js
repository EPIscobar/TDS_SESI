let MaiorDeIdade = true
let possuiConvite = false

let verificadoAND = "voce possui acesso?";
verificadoAND += "\n" + (MaiorDeIdade && possuiConvite);
console.log(verificandoAND);
if(verificandoAND = false){
    console.log("voce possui o convite, entrada autorizada")
}
else{
    console.log("acesso negado, voce nao possui o onvite VIP")
    
}